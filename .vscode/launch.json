{
  "configurations": [
    {
      "name": "C/C++: cl.exe build and debug active file",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${fileDirname}\\${fileBasenameNoExtension}.exe",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${fileDirname}",
      "environment": [],
      // "console": "internalConsole", // Sometimes Pop-up CMD?
      "console": "integratedTerminal", // But lose focus. For I/O stream. Or use extension CodeLLDB https://stackoverflow.com/a/73044327
      "preLaunchTask": "${defaultBuildTask}"
    },

  ],
  "version": "2.0.0"
}
